#include "main.h"
/**
 * add - adds the two variables and returns the result
 *
 * @a: variable, check the number
 * @b: variable, check the number
 *
 * Return: returns the result of adding a and b
 */
int add(int a, int b)
{
	return (a + b);
}
