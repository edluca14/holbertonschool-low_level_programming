#include "main.h"
/**
 * _abs - Gives the absolute value of a number
 *
 * @n: the number selected
 *
 * Return: Absolute value of n
 *
 */
int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	else
	{
		return (-n);
	}
}
